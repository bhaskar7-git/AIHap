{
  "name": "smartqueue-monorepo",
  "version": "1.0.0",
  "description": "SmartQueue - Digital Hospital Appointment, Patient Token & Smart Queue Management System",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "seed": "npm --prefix backend run seed",
    "test": "npm --prefix backend run test:e2e"
  },
  "keywords": ["hospital", "queue", "tokens", "appointment", "hackathon", "smartqueue"],
  "author": "SmartQueue Team",
  "license": "MIT"
}
